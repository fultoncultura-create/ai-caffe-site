export const metadata = {
  title: "AI Caffè",
  description: "Portfólio de projetos audiovisuais com IA + Cinema",
};

export default function RootLayout({ children }) {
  return (
    <html lang="pt-BR">
      <body>{children}</body>
    </html>
  )
}
